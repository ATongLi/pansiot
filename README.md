# Claude Code Workflow Template

完整的项目模板，用于 Claude Code 结构化研发工作流管理体系。

## 🎯 特性

- ✅ **单功能独立推进**: 每个功能按 REQ → FE → SOL → IMP → 实现 独立推进
- ✅ **并行任务支持**: 多个功能可以在不同阶段并行开发
- ✅ **依赖忽略机制**: 通过 mock/stub 并行开发依赖模块，完成后快速补齐
- ✅ **精准代码映射**: 功能到代码的完整映射，支持精准修改
- ✅ **完整追溯体系**: 从需求到代码到测试的双向追溯矩阵
- ✅ **多平台支持**: 网关、HMI、组态、云平台、APP、边缘AI、Scada、Web编辑器

## 📋 支持的平台

- 网关端 (Gateway)
- HMI运行端 (HMI)
- 组态端 (Configuration)
- 云平台端 (Cloud)
- APP端 (APP)
- 边缘智能服务器 (Edge AI)
- Scada软件
- Web可视化编辑器 (Web Editor)

## 🚀 快速开始

### 方式1: 使用脚本初始化（推荐）

```bash
# 1. 克隆或下载模板
git clone <repository-url> my-project
cd my-project

# 2. 运行初始化脚本
python .claude-workflow/scripts/init-workflow.py \
    --project-name "我的项目" \
    --project-type "iot-platform" \
    --platforms "gateway,cloud,hmi" \
    --template-source .
```

### 方式2: 手动设置

```bash
# 1. 复制整个 .claude-workflow 目录到你的项目
cp -r claude-workflow-template/.claude-workflow my-project/

# 2. 复制 skills 到你的项目
cp -r claude-workflow-template/skills my-project/

# 3. 配置项目
# 编辑 .claude-workflow/config.yml

# 4. 启动 Claude Code
claude-code .
```

## 📖 使用方式

### 开始第一个功能

```bash
# 启动 Claude Code
claude-code .

# 对 Claude Code 说:
"我们需要添加用户认证功能"

# workflow-orchestrator 自动激活并引导你完成:
# 1. 收集需求 (requirement-manager)
# 2. 设计方案 (solution-designer)
# 3. 制定实现计划 (implementation-manager)
# 4. 实现代码 (implementation-manager)
# 5. 验证和文档 (verification-manager, documentation-generator)
```

### 日常开发场景

#### 场景1: 开发新功能
```
"添加[新功能]"
→ workflow-orchestrator 自动激活
→ 完整的流程引导
```

#### 场景2: 修改现有功能
```
"修改 FE-001-02 的密码验证逻辑"
→ implementation-manager 查询映射表
→ 显示影响分析
→ 执行精准修改
→ 更新追溯
```

#### 场景3: 并行开发多个功能
```
"同时开发 FE-001 和 FE-002"
→ workflow-coordinator 管理并行任务
→ 独立推进，互不阻塞
```

#### 场景4: 处理依赖
```
"FE-002 依赖 FE-001，但想并行开发"
→ workflow-coordinator 建议使用依赖忽略
→ 在代码中标记 TODO(依赖)
→ 实现主要逻辑
→ 等 FE-001 完成后快速补齐
```

#### 场景5: 暂停和恢复
```
"暂停当前任务，处理 IMP-002"
→ 任务切换和上下文保存
"继续之前的任务"
→ 恢复到之前的任务
```

## 📁 项目结构

```
.claude-workflow/                      # 工作流管理目录
├── 01-requirements/                    # 需求管理
│   ├── raw-requirements/              # 原始需求 (REQ-XXX)
│   ├── functional-requirements/       # 功能需求 (FE-XXX)
│   └── user-stories/                  # 用户故事 (US-XXX)
├── 02-planning/                       # 计划管理
├── 03-design/                         # 设计管理
│   ├── architecture/                  # 架构设计
│   ├── technical-solutions/           # 技术方案 (SOL-XXX)
│   ├── api-design/                    # API设计
│   └── database-design/               # 数据库设计
├── 04-implementation/                  # 实现管理
│   ├── implementation-plans/          # 实现计划 (IMP-XXX)
│   ├── code-mapping/                  # 代码映射
│   └── implementation-logs/           # 实现日志
├── 05-verification/                   # 验证管理
├── 06-documentation/                  # 文档管理
├── 07-platforms/                      # 多平台管理
│   ├── gateway/                       # 网关端
│   ├── hmi/                           # HMI运行端
│   ├── configuration/                 # 组态端
│   ├── cloud/                         # 云平台端
│   ├── app/                           # APP端
│   ├── edge-ai/                       # 边缘智能服务器
│   ├── scada/                         # Scada软件
│   └── web-editor/                    # Web可视化编辑器
├── parallel-tasks/                     # 并行任务管理
│   ├── active-tasks.md                # 活跃任务列表
│   └── task-dependencies.md           # 任务依赖关系
├── templates/                          # 文档模板
├── config.yml                          # 项目配置
├── current-phase.md                    # 当前执行阶段
├── feature-to-code-map.md             # 功能代码映射
├── dependency-backlog.md              # 依赖跟踪
├── rt-matrix.md                        # 需求追溯矩阵
└── scripts/                            # 初始化脚本
```

## 🔧 配置

编辑 `.claude-workflow/config.yml`:

```yaml
project:
  name: "物联网平台"
  type: "iot-platform"
  version: "1.0.0"

platforms:
  - id: gateway
    name: 网关端
    enabled: true
  # ... 其他平台

workflow:
  enforce_order: true          # 强制执行流程顺序
  enable_parallel: true         # 允许并行任务
  max_parallel_tasks: 5         # 最大并行任务数
  enable_dependency_ignore: true  # 启用依赖忽略机制
```

## 📚 Skills

项目包含以下 Claude Code Skills:

- **workflow-orchestrator**: 主控技能，管理完整工作流
- **requirement-manager**: 需求收集和管理
- **solution-designer**: 技术方案设计
- **implementation-manager**: 实现计划和管理
- **workflow-coordinator**: 任务协调和状态管理

### 安装 Skills

```bash
# 自动安装
cd .claude-workflow/skills
./install-skills.sh  # Linux/Mac
# 或
install-skills.bat   # Windows

# 或手动复制 skills/* 到 Claude Code skills 目录
# Windows: %APPDATA%\Claude\skills\
# Mac/Linux: ~/.config/claude/skills/
```

## 📖 核心概念

### 单功能生命周期

```
REQ-{N} (原始需求)
    ↓
FE-{N} (功能需求) ← 独立推进点
    ↓
SOL-{N} (技术方案) ← 独立推进点
    ↓
IMP-{N} (实现计划) ← 独立推进点
    ↓
代码实现 ← 独立推进点
    ↓
验证和文档 ← 独立推进点
```

### 并行开发示例

```
时间线 →

REQ-001 ━━→ FE-001 ━━→ SOL-001 ━━→ IMP-001 ━━→ 实现
            ↓
REQ-002 ━━━━━━→ FE-002 ━━→ SOL-002 ━━→ IMP-002 ━━→ 实现
                     ↓
REQ-003 ━━━━━━━━━━━━→ FE-003 ━━→ SOL-003 ━━→ IMP-003
```

### 依赖忽略机制

```typescript
// TODO(依赖): FE-001 - 用户认证
// 说明: 此处需要调用认证服务验证用户身份
// 当前状态: 使用 mock 实现
// 依赖模块: IMP-001
// 补齐优先级: P0
// 预计补齐日期: 2025-01-20
// 负责人: @username

export class DataCollector {
  async collectData(userId: string) {
    // === 依赖忽略开始 ===
    // Mock 实现:
    const user = { id: userId, name: 'Mock User' };
    // === 依赖忽略结束 ===

    // 主要业务逻辑继续实现
    const data = await this.fetchData();
    return this.processData(data);
  }
}
```

## 📄 模板文档

### 需求模板

- `REQ-template.md`: 原始需求模板
- `FE-template.md`: 功能需求模板
- `SOL-template.md`: 技术方案模板
- `IMP-template.md`: 实现计划模板

### 状态管理模板

- `active-tasks-template.md`: 活跃任务列表
- `task-dependencies-template.md`: 任务依赖关系
- `dependency-backlog-template.md`: 依赖跟踪
- `feature-to-code-map-template.md`: 功能代码映射
- `rt-matrix-template.md`: 需求追溯矩阵

## 🔍 常用命令

### 查询状态
```
"我上次做到哪了？"              → 显示当前任务和进度
"显示所有并行任务"              → 列出所有活跃任务
"检查依赖状态"                  → 显示依赖和待补齐项
```

### 任务管理
```
"暂停当前任务，处理 IMP-002"    → 切换任务
"继续之前的任务"                → 恢复任务
"验证 FE-001 是否可以进入实现"  → 验证阶段转换
```

### 代码操作
```
"修改 FE-001-02 的验证逻辑"     → 精准修改
"显示 FE-001 的代码映射"        → 查询映射表
"分析修改影响"                  → 影响分析
```

## 🎓 最佳实践

### ✅ 推荐做法

- 使用脚本初始化项目
- 将所有工作流文件纳入版本控制
- 遵循标准命名规范 (REQ-XXX, FE-XXX, SOL-XXX, IMP-XXX)
- 完整记录需求、设计、实现
- 及时更新追溯矩阵
- 使用依赖忽略机制提高并行效率
- 定期备份工作流状态

### ❌ 避免做法

- 跳过工作流阶段直接实现
- 忽略文档完整性验证
- 手动修改状态文件（让 CC 管理）
- 使用不一致的命名
- 遗留未补齐的依赖忽略
- 忘记更新代码映射表
- 在未完成验证时合并代码

## 📚 文档

完整的设计方案请参考:
- [Claude-Code-Workflow-Design-v1.0.0.md](./Claude-Code-Workflow-Design-v1.0.0.md)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

---

**版本**: v1.0.0
**最后更新**: 2025-01-19
**维护者**: Claude Code Workflow Team
